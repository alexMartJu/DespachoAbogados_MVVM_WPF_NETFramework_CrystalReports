<UserControl x:Class="SistemaGestionDespacho.View.Views.InformesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SistemaGestionDespacho.View.Views"
             xmlns:cr="clr-namespace:SAPBusinessObjects.WPF.Viewer;assembly=SAPBusinessObjects.WPF.Viewer"
             mc:Ignorable="d" 
             d:DesignWidth="1200" d:DesignHeight="700"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Estilo para los botones de acción en la parte izquierda -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,4,0,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                <Setter TargetName="border" Property="Background" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Layout principal: dos columnas, botones a la izquierda y visor de informes a la derecha -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Botones de acción en la parte izquierda -->
        <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0" VerticalAlignment="Top">
            <Button Content="📄 Listado de Clientes" Background="#2563EB" Style="{StaticResource ActionButton}" Command="{Binding MostrarInformeClientesCommand}" MinWidth="170"/>
            <Button Content="📊 Expedientes por Estado" Background="#10B981" Style="{StaticResource ActionButton}" Command="{Binding MostrarInformeExpedientesCommand}" MinWidth="170"/>
            <Button Content="📝 Actuaciones por Expediente" Background="#F59E0B" Style="{StaticResource ActionButton}" Command="{Binding MostrarInformeActuacionesCommand}" MinWidth="170"/>
        </StackPanel>

        <!-- Crystal Reports viewer -->
        <Border Grid.Column="1" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
            <cr:CrystalReportsViewer x:Name="reportViewer"/>
        </Border>
    </Grid>
</UserControl>
